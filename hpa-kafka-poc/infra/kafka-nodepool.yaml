apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaNodePool
metadata:
  name: poc-kafka-pool
  namespace: kafka
  labels:
    strimzi.io/cluster: poc-kafka
spec:
  replicas: 1
  roles:
    - controller
    - broker
  storage:
    type: ephemeral
